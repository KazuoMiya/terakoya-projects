# 最終課題で使う外部パッケージ。venv を有効にしてから:
# External packages for the final project. After activating the venv:
#   pip install -r requirements.txt

# HTTPチェックと、課題5のAPIへの記録に使う（課題3の通知でも使った）。
# Used for the HTTP checks and for recording to the Project 5 API (also used for Project 3's notifications).
requests>=2.31,<3

# .env（API_KEY）を読み込む道具。課題2と同じ。
# Loads .env (API_KEY). Same as Project 2.
python-dotenv>=1.0,<2
