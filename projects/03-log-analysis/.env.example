# このファイルを .env にコピーして使う（cp .env.example .env）。
# .env は .gitignore 済みなのでコミットされない。

# Slack Incoming Webhook の URL。
# これは「URLの形をしたパスワード」だ——知っていれば誰でもそのチャンネルに投稿できる。
# コードに書かない・コミットしない・チャットに貼らない。
#
# Slack を使える環境が無ければ空のままでよい（ツールは通知本文を表示するだけになる）。
SLACK_WEBHOOK_URL=
