# 課題5で使う外部パッケージ。venv を有効にしてから:
# External packages for Project 5. After activating the venv:
#   pip install -r requirements.txt

# FastAPI = Web APIの骨組み。型を書くと、検証とAPIの説明書（/docs）がついてくる。
# FastAPI = the Web API framework. Write the types and you get validation and API docs (/docs) for free.
fastapi>=0.110,<1

# uvicorn = 開発用のWebサーバー。uvicorn app:app --reload --app-dir src で起動する。
# uvicorn = the development web server. Start it with: uvicorn app:app --reload --app-dir src
uvicorn>=0.29,<1

# httpx = テスト道具 TestClient が内部で使う（サーバーを起動せずにAPIを呼べる）。
# httpx = used internally by the TestClient test tool (calls the API without starting a server).
httpx>=0.27,<1

# .env（API_KEY など）を読み込む道具。課題2と同じ。
# Loads .env (API_KEY etc.). Same as Project 2.
python-dotenv>=1.0,<2
